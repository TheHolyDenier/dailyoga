---
interface Props {
  name: string;
  width: number;
  height: number;
}

const { name, width, height } = Astro.props;
const src = `/svg/${name.toLowerCase().split(" ").join("-")}.svg`;
---

<img alt={name} src={src} width={width} height={height} />
